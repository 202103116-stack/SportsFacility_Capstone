# 프로젝트 배경

한국외국어대학교 Global Sports Industry + AI 융합 복수전공 4학년(졸업학기) 전공필수 캡스톤
「스포츠융합산업프로젝트실습」(S01418201, 26-2학기) 팀 프로젝트입니다.
중간발표(8주차)에서 주제 제안, 기말(14~15주차)에 최종 결과물을 발표합니다.

이 프로젝트를 진행하는 나(사용자)는 스포츠산업과 AI를 함께 전공하고 있고, 동시에
제조/생산/품질 관리 직무(양산관리) 쪽으로도 취업을 준비하고 있습니다. 그래서 이번
캡스톤은 "스포츠 산업 과제"이면서 동시에 "AI 기반 데이터분석 역량을 제조/품질 도메인에도
그대로 옮겨 쓸 수 있는 연습"이 되기를 바라는 이중 목적이 있습니다. 보고서/발표 자료에
특정 회사나 채용 맥락을 직접 언급하진 않지만, 작업 방향을 잡을 때는 이 배경을 계속
염두에 두고 조언해주세요.

# 주제

**전국 체육시설 안전점검 데이터를 활용한 시설 안전위험 예측 및 점검자원 우선순위 최적화**

핵심 스토리 흐름 (세 단계를 하나로 엮어서 진행):
1. **예측**: 시설 특성(업종, 시설유형, 지역, 면적, 시설연령, 마지막 점검 이후 경과일수 등)으로
   안전위험군을 예측하는 분류 모델을 만든다. **(EDA로 확정된 타겟 정의: 이진분류 —
   양호=0 vs 위험군(주의+사용중지)=1. "수리필요" 등급은 실제 데이터에 존재하지 않고,
   `사용중지`는 전국 23건뿐이라 3-클래스 분류가 불가능함을 확인 — `docs/eda_findings.md` §2,
   `docs/WORKFLOW.md` §1 참고.)**
2. **최적화**: 그 예측 결과를 바탕으로, 한정된 점검 인력/예산을 어떤 시설부터 우선 투입할지
   우선순위를 정하는 자원배분 로직을 설계한다.
3. **검증**: 스포츠안전사고 통계(스포츠안전재단)와 교차검증해서, 모델이 위험하다고 예측한
   업종/지역이 실제 사고 통계 경향과 방향이 맞는지 확인한다.

전체 워크플로우(문제정의~모니터링 9단계)와 각 단계별 진행 상태는 `docs/WORKFLOW.md` 참고.
특히 `TODZ_API_FACI_SAFETY`가 시설당 "최신 점검 1건"만 제공한다는 구조적 한계 때문에
"예측"이라는 표현을 쓸 때 범위를 어떻게 정직하게 좁혀야 하는지가 `docs/WORKFLOW.md` §3에
정리되어 있음 — 모델링 단계 진입 전 반드시 확인.

## 왜 이 주제로 좁혔는가 (중요 — 항상 이 관점 유지)

이전에 검토했던 다른 후보 주제들(부상예측, 수요예측 등)은 데이터가 소규모·합성이거나
실측 데이터가 부족한 문제가 있었다. 반면 이 주제는 정부가 실제로 운영하는 오픈API +
CSV로 검증된 15만 건 이상의 실데이터가 있어서, "주제가 데이터에 맞춰 좁혀지는" 것이 아니라
"데이터가 감당할 수 있는 견고한 주제로 좁혀지는" 건강한 순서를 따른다.

또한 이 주제를 의도적으로 고른 이유는, **스포츠 도메인이라는 틀 위에 얹혀 있지 않아도
그 자체로 통하는 범용 문제 구조**이기 때문이다. 예를 들어 "한정된 자원(인력/예산)으로 다수의 대상
(시설/설비/라인)을 얼마나 자주, 어떤 순서로 점검할 것인가"라는 문제는 제조업의
예지보전(predictive maintenance), 품질관리에서의 검사 우선순위 결정 문제 등과도 구조적으로
동일하다. "시설명"을 "설비ID"로, "안전등급"을 "불량확률/고장확률"로만 바꾸면 그대로
양산관리 문제가 된다. 데이터 전처리 과정(결측치 처리, 이력 데이터 중복 제거, 불균형
라벨 이진분류)도 품질관리 실무에서 반복적으로 마주치는 패턴이라 연습 가치가 크다.

**즉 앞으로 방법론이나 모델링 방향을 제안할 때는, "스포츠 시설이라서 특수한 방법"보다는
"범용 산업 안전/품질 예측 문제에 그대로 적용 가능한 방법"을 우선적으로 고려해줘.**
이게 이 프로젝트를 다른 순수 스포츠 주제와 차별화하는 핵심 포인트다.

# 데이터

전체 목록과 검증 결과는 `docs/data_sources.md` 참고. 메인 테이블(`TODZ_API_FACI_SAFETY`)의
컬럼별 정의·결측률·데이터 품질 이슈는 `docs/data_dictionary.md`에 실측 기반으로 정리되어
있음 — 전처리/피처엔지니어링 작업 전 반드시 확인. 핵심만 요약:

- **체육시설안전정보** (오픈API `TODZ_API_FACI_SAFETY`, 시설당 1행, 약 98,551건) — 메인 모델링 후보
- **안전점검결과 이력** (오픈API `TODZ_API_FACI_SCHK`, 점검 건별 이력, 약 267,694건) — 시계열/이력 파생변수용
- **전국체육시설 일반정보** (오픈API `TODZ_API_SFMS_FACI`, 약 153,494건) — `faci_cd` 기준 조인 후보 (샘플 대조로 두 데이터셋 간 `faci_cd` 교차 매칭이 실제로 존재함을 확인함 — 정확한 매칭률은 전체 데이터 수집 후 `src/`에 검증 스크립트로 확정 필요)
- 기존 보유 CSV `data/` 또는 원본 업로드분(`KS_ALSFC_SAFECHK_INFO_202607.csv` 등) — 오픈API와 필드 1:1 대응 확인됨, 단 시설 마스터+이력이 섞여 있어 중복 많음(약 40%)
- 보조 데이터: 국민체력100(정상범위 참고), 스포츠안전사고 통계(교차검증용), 체육시설 인접 대중교통정보, 전국공공체육시설현황

API 인증키와 엔드포인트는 `.env`에 있음 (`DATA_GO_KR_SERVICE_KEY`, `FACILITY_INFO_ENDPOINT`, `FACILITY_SAFETY_ENDPOINT`). 데이터 수집은 `src/fetch_facility_data.py` 사용.

실행 환경은 프로젝트 루트의 `.venv`(Python 3.11) 사용 — `.venv\Scripts\activate` 후
`pip install -r requirements.txt`. 시스템 기본 `python`/`py` 명령은 버전이 여러 개
혼재해 있어 그대로 쓰면 패키지 누락 에러가 날 수 있음.

# 폴더 구조

```
data/raw          원본 데이터 (API pull, CSV 원본)
data/processed    전처리 완료 데이터
data/external      보조 데이터
notebooks          EDA·모델링 노트북
src                재사용 스크립트 (수집/전처리/모델링)
models             학습된 모델
outputs/figures    시각화 결과물
outputs/reports    보고서/발표자료 산출물
docs               데이터 출처, 계획서, 회의록
```

# 앞으로의 작업 방향

상세 진행 상태와 단계별 파일 매핑은 `docs/WORKFLOW.md` 참고. 요약:

1. ~~데이터 전체 수집~~ ✅ (`data/raw/facility_safety_20260917.csv`, 98,551건)
2. ~~EDA — 결측치/이상치/타겟 분포 확인~~ ✅ (`docs/eda_findings.md`, `notebooks/01_eda.ipynb`,
   `outputs/figures/eda/`)
3. **다음 단계: Validation design** — stratified split/k-fold 설계 (`docs/WORKFLOW.md` §4)
4. 전처리/피처엔지니어링 — `src/preprocess.py`(예정), `docs/data_dictionary.md` §4의 품질
   이슈 반영
5. 모델링 — baseline(로지스틱) → 트리 기반 앙상블, Recall/Precision/F1/PR-AUC 우선
6. 자원배분 최적화 — 예측 위험도 기반 점검 우선순위 스코어링 로직 설계
7. 검증 — 스포츠안전사고 통계와 교차검증
8. 발표자료/보고서 정리 (`docs/presentation_script.md`, `outputs/reports/`)

`faci_cd` 기준 `TODZ_API_SFMS_FACI` 조인은 필수 경로가 아님 — 샘플 검증 결과 핵심 예측
피처가 이미 `TODZ_API_FACI_SAFETY`에 포함되어 있어 EDA 단계에서는 조인 없이 진행함.

작업할 때마다 이 파일(CLAUDE.md)의 맥락을 기준으로 판단하고, 스포츠 도메인에 매몰되지
않고 범용 산업 AI 문제로도 설명 가능한 방향을 우선 제안해줘.
